# Contributing
1. Open an issue before changing the permission model or adding a connector.
2. Fork, branch from `main`, and install with `pip install -e '.[dev]'`.
3. Run `ruff check src tests`, `pytest`, and `python -m build`.
4. Keep changes small, document trust-boundary changes, and include tests.
5. Use clear commit messages and complete the pull-request checklist.
