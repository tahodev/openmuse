# Security
OpenMuse is alpha software with no production-supported release. Report vulnerabilities using a private GitHub security advisory, never a public issue.

Current limits: tools share the Python process; URL filtering does not yet pin DNS across connection; the audit log is hash-chained but not externally anchored; approvals need a separate human UI; secrets are not encrypted by OpenMuse. Use test accounts and non-sensitive data.
